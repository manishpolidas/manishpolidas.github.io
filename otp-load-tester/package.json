{
  "name": "otp-load-tester",
  "version": "1.0.0",
  "private": true,
  "description": "OTP testing and load-testing platform for authorized OTP systems (mock SMS by default)",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=server & npm run dev --workspace=web",
    "build": "npm run build --workspace=server && npm run build --workspace=web",
    "test": "npm run test --workspace=server",
    "test:e2e": "npm run test:e2e --workspace=web",
    "typecheck": "npm run typecheck --workspace=server && npm run typecheck --workspace=web",
    "hash-password": "npm run hash-password --workspace=server --"
  },
  "engines": {
    "node": ">=20.12.0"
  }
}
